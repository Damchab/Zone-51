define([], function() {
    function Toolfunction() {
    }

    Toolfunction.prototype.routing = function(file, functionSuccess,
            functionFail) {
    };

    return new Toolfunction();
});